# OpenAI API key (required when using the openai provider)
OPENAI_API_KEY=

# Groq API key (required when using the groq provider)
GROQ_API_KEY=

# Port for the Express backend server (default: 3001)
PORT=3001
