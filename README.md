# AIWrapper
AI-Resume-Validator An intelligent resume screening wrapper that enforces strict ATS formatting standards. It automatically rejects non-compliant resumes (multi-columns, images, non-standard headers) and provides a weighted 0-5 scoring system based on semantic skill matching and quantifiable impact metrics.
